{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn server:app --reload --host 0.0.0.0 --port 8000",
    "dev:8001": "uvicorn server:app --reload --host 0.0.0.0 --port 8001"
  }
}
